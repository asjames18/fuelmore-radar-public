# Agent entry point

Read and follow [`AGENTS.md`](AGENTS.md). It is the canonical instruction file for
AI agents working in `D:\fuelmore-radar`.

The detailed project handoff is [`docs/PROJECT_OVERVIEW.md`](docs/PROJECT_OVERVIEW.md).
Do not duplicate the canonical rules here; update `AGENTS.md` instead.

